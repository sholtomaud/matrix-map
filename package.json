{
    "name": "conceptual-matrix-mapper-v3",
    "private": true,
    "version": "3.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "check": "tsc --noEmit",
        "test": "playwright test",
        "validate": "npm run check && npm run build && npm run test"
    },
    "devDependencies": {
        "@playwright/test": "^1.40.0",
        "typescript": "^5.3.0",
        "vite": "^8.0.16"
    }
}